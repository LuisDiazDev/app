name: app
description: testPractico

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  http: ^0.12.0+2
  #BASEDEDATOS
  sqflite: ^1.1.6+4
  path_provider: ^1.6.5
  json_to_form: ^0.0.2
  crypto: ^2.1.2

  #CHARTS
  charts_flutter: ^0.9.0

  #TEST
  test: ^1.6.5
  scoped_model: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true

